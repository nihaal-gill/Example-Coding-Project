This program is class that the user has to intilize a box or mutliple boxes and it returns various details about the box. First the user
has to set the dimensions of the box. The user has to set its x,y,z components of the center, width, height, and depth; these are 
also the parameters of the class in that order. Once that is initialized the user can find out what the volume and surface area of the 
box are. As well as if the user has created two boxes then it can also find out if the two boxes overlap or not.
